@namespace Juegardos.Website.Components

<div>
    <button class="@(bigButton ? "big-button" : "small-button") moreinfo-button"><i class="material-icons">info_outline</i>Más información</button>
</div>

@code {
    [Parameter]
    public bool bigButton { get; set; } = true;
}
